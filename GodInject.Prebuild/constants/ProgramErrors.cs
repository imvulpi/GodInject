namespace GodInject.Prebuild.constants
{
    /// <summary>
    /// Enum for fatal program errors which signify the program can't continue further.
    /// </summary>
    public enum ProgramErrors
    {
        None = 0,
        Unknown,
        InvalidArgs,
    }
}
